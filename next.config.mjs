/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/e-commerce-pempek-bu-ida',
  assetPrefix: '/e-commerce-pempek-bu-ida/',
};

export default nextConfig;
